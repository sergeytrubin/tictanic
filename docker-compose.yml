version: '2'

services:
  website:
    build: .
    command: >
      gunicorn -b 0.0.0.0:5000
        --access-logfile -
        --reload
        "tictanic.app:create_app()"
    environment:
      PYTHONBUFFERED: 'true'
    volumes:
      - '.:/tictanic'
    ports:
      - '5000:5000'